import { NavigationContainer } from '~/modules';

import { BottomTabs } from './bottomTabs';

export const AppRoutes = () => {
  return (
    <NavigationContainer>
      <BottomTabs />
    </NavigationContainer>
  );
};
